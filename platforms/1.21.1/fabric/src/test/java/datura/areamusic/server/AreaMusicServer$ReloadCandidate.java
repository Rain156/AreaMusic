package datura.areamusic.server;

/**
 * Deliberately unsafe test-output shadow for the production nested class with this binary name.
 * A dedicated-server gate must read explicit production roots and therefore never see this type.
 */
final class AreaMusicServer$ReloadCandidate {
    private net.minecraft.client.Minecraft clientOnly;
}
